{
  "name": "pos-hono",
  "version": "1.0.0",
  "description": "Simple POS (point-of-sale) REST API built with Hono. Centerpiece: a transactional checkout engine.",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "seed:demo": "bun run src/seed-demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.27",
    "pg": "^8.22.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/pg": "^8.20.0",
    "typescript": "^6.0.3"
  }
}
